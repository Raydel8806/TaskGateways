import { PeriphericalDevice } from './peripherical-device';

describe('PeriphericalDevice', () => {
  it('should create an instance', () => {
    expect(new PeriphericalDevice()).toBeTruthy();
  });
});
